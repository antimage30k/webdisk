server {
    client_max_body_size 4G;
    listen 7777;

    location /disk {
        proxy_pass http://localhost:4000/disk;
    }

    location / {
        rewrite ^.*$ /disk/index redirect;
    }

    location /download {
        alias /home/webdisk;
    }
}